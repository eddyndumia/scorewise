# Copy to .env if you need to override the backend URL. The frontend never
# talks to Supabase directly — auth is brokered entirely through the FastAPI
# backend (see ../pesascore-backend/.env.example) via httpOnly cookies.

VITE_API_BASE_URL=http://localhost:8000
